spring:
  datasource:
    url: jdbc:h2:mem:validation;DB_CLOSE_DELAY=-1;MODE=Oracle
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
  sql:
    init:
      mode: always
  data:
    mongodb:
      uri: mongodb://localhost:27017/clearflow
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD:clearflow123}
  kafka:
    bootstrap-servers: localhost:29092
  artemis:
    broker-url: tcp://localhost:61616
    user: ${ARTEMIS_USER:clearflow}
    password: ${ARTEMIS_PASSWORD:clearflow123}

clearflow:
  embargo:
    countries: IR,KP,SY,CU,SD,MM,RU

camel:
  component:
    kafka:
      brokers: localhost:29092