spring:
  application:
    name: validation-enrichment
  cloud:
    vault:
      uri: ${VAULT_ADDR:http://localhost:8200}
      authentication: TOKEN
      token: ${VAULT_TOKEN:dev-root-token}
      kv:
        enabled: true
        backend: clearflow
        default-context: validation-enrichment
      fail-fast: false
    config:
      import-check:
        location-not-found: allow
